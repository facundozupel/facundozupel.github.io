---
import Layout from '../../layouts/Layout.astro';

const cascadas = [
  { nombre: 'Salto del Laja', slug: 'salto-del-laja', region: 'Biobío', altura: 35, dificultad: 'Fácil', img: '/assets/cascadas/salto-del-laja/hero.webp', imgAlt: 'Vista panorámica del Salto del Laja con sus cuatro brazos sobre roca basáltica' },
  { nombre: 'Cascada de las Ánimas', slug: 'cascada-de-las-animas', region: 'Metropolitana', altura: 60, dificultad: 'Fácil a moderado', img: '/assets/cascadas/cascada-de-las-animas/hero.webp', imgAlt: 'Santuario de la naturaleza Cascada de las Ánimas en el Cajón del Maipo' },
  { nombre: 'Salto del Itata', slug: 'salto-del-itata', region: 'Ñuble', altura: 75, dificultad: 'Fácil a moderado', img: '/assets/cascadas/salto-del-itata/hero.webp', imgAlt: 'Salto del Itata, cascada de 75 metros en la Región de Ñuble' },
  { nombre: 'Saltos del Petrohué', slug: 'saltos-del-petrohue', region: 'Los Lagos', altura: 20, dificultad: 'Fácil', img: '/assets/cascadas/saltos-del-petrohue/hero.webp', imgAlt: 'Saltos del Petrohué con agua turquesa sobre roca basáltica volcánica' },
];

const dificultadColor: Record<string, string> = {
  'Fácil': '#00e5d4',
  'Fácil a moderado': '#b8e8e4',
  'Moderada': '#b8e8e4',
  'Difícil': '#f4efe6',
};
---

<Layout
  title="Cascadas de Chile — Guías completas con cómo llegar, qué hacer y dónde alojarse"
  description="Descubre las cascadas más impresionantes de Chile con guías completas: cómo llegar, actividades, alojamiento, clima y toda la información para tu visita."
>

  <!-- ─── HERO ──────────────────────────────────────────────────────────────── -->
  <section style="
    background:#091d2b;
    padding: 10rem 2.5rem 5rem;
    position:relative; overflow:hidden;
  ">
    <!-- Noise texture -->
    <div style="
      position:absolute; inset:0; z-index:0; pointer-events:none;
      background-image: url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 width=%22200%22 height=%22200%22><filter id=%22n%22><feTurbulence type=%22fractalNoise%22 baseFrequency=%220.65%22 numOctaves=%223%22 stitchTiles=%22stitch%22/><feColorMatrix type=%22saturate%22 values=%220%22/></filter><rect width=%22200%22 height=%22200%22 filter=%22url(%23n)%22 opacity=%220.03%22/></svg>');
    "></div>

    <div style="position:relative; z-index:1;">
      <!-- Breadcrumb -->
      <nav style="margin-bottom:2rem;">
        <a href="/" style="color:rgba(244,239,230,0.5); text-decoration:none; font-size:0.78rem; letter-spacing:0.08em; text-transform:uppercase;">Inicio</a>
        <span style="color:rgba(244,239,230,0.25); margin:0 0.5rem;">›</span>
        <span style="color:#00e5d4; font-size:0.78rem; letter-spacing:0.08em; text-transform:uppercase;">Cascadas</span>
      </nav>

      <div style="display:flex; align-items:flex-end; justify-content:space-between; gap:2rem; flex-wrap:wrap;">
        <h1 style="
          font-family:'Archivo Black',sans-serif;
          font-size:clamp(4rem, 12vw, 14rem);
          color:#f4efe6; margin:0; line-height:0.88;
          text-transform:uppercase; letter-spacing:-0.02em;
        ">
          TODAS<br/><span style="color:#00e5d4;">LAS</span><br/>CASCADAS
        </h1>
        <div style="padding-bottom:1rem; text-align:right;">
          <p style="
            font-family:'Archivo Black',sans-serif;
            font-size:clamp(2.5rem, 5vw, 5rem);
            color:rgba(244,239,230,0.15); margin:0; line-height:1;
          ">{cascadas.length}</p>
          <p style="color:rgba(244,239,230,0.35); font-size:0.8rem; letter-spacing:0.12em; text-transform:uppercase; margin:0.25rem 0 0;">con guía completa</p>
        </div>
      </div>
    </div>
  </section>


  <!-- ─── FILTRO DIFICULTAD ────────────────────────────────────────────────── -->
  <section style="background:#091d2b; padding:0 2.5rem 4rem;">
    <div style="
      border-top:1px solid rgba(244,239,230,0.08);
      padding-top:2rem;
      display:flex; gap:0.75rem; align-items:center;
    ">
      <span style="color:rgba(244,239,230,0.35); font-size:0.75rem; letter-spacing:0.12em; text-transform:uppercase; margin-right:0.5rem;">Dificultad:</span>
      {['Todas', 'Fácil', 'Moderada', 'Difícil'].map((d, i) => (
        <button style={`
          background:${i === 0 ? '#00e5d4' : 'transparent'};
          color:${i === 0 ? '#091d2b' : 'rgba(244,239,230,0.6)'};
          border:1px solid ${i === 0 ? '#00e5d4' : 'rgba(244,239,230,0.15)'};
          font-size:0.75rem; font-weight:600; letter-spacing:0.1em;
          text-transform:uppercase; padding:0.4rem 1.1rem; border-radius:999px;
          cursor:pointer;
        `}>{d}</button>
      ))}
    </div>
  </section>


  <!-- ─── GRID DE CASCADAS ──────────────────────────────────────────────────── -->
  <section style="background:#091d2b; padding:0 2.5rem 8rem;">
    <div class="cascadas-grid">
      {cascadas.map(c => (
        <a href={`/cascadas/${c.slug}`} class="cascade-card" style="display:block; text-decoration:none;">
          <div class="cascade-card__img-wrap">
            <img src={c.img} alt={c.imgAlt} loading="lazy" width="800" height="533" decoding="async" class="cascade-card__img" />
            <span style={`
              position:absolute; top:1rem; left:1rem;
              background:${dificultadColor[c.dificultad] || '#00e5d4'}; color:#091d2b;
              font-size:0.7rem; font-weight:700; letter-spacing:0.1em;
              text-transform:uppercase; padding:0.3rem 0.75rem; border-radius:999px;
            `}>{c.dificultad}</span>
            <span style="
              position:absolute; bottom:1rem; right:1rem;
              background:rgba(9,29,43,0.75); color:#f4efe6;
              font-family:'Archivo Black',sans-serif; font-size:1rem;
              padding:0.3rem 0.65rem; border-radius:4px;
            ">{c.altura}m</span>
          </div>
          <h2 style="
            font-family:'Archivo Black',sans-serif;
            font-size:1.4rem; color:#f4efe6; margin:0 0 0.3rem;
            text-transform:uppercase;
          ">{c.nombre}</h2>
          <p style="color:rgba(244,239,230,0.45); font-size:0.88rem; margin:0; font-weight:300;">{c.region}</p>
        </a>
      ))}
    </div>
  </section>


  <!-- ─── CTA MAPA ─────────────────────────────────────────────────────────── -->
  <section style="
    background:#00e5d4; padding:6rem 2.5rem; text-align:center;
  ">
    <h2 style="
      font-family:'Archivo Black',sans-serif;
      font-size:clamp(2rem, 5vw, 5rem);
      color:#091d2b; margin:0 0 1.5rem; line-height:0.95;
      text-transform:uppercase;
    ">Búscalas en el Mapa</h2>
    <p style="color:#091d2b; font-size:1.05rem; max-width:480px; margin:0 auto 2.5rem; font-weight:300; opacity:0.7;">
      Usa el mapa interactivo para encontrar cascadas cerca de donde estás o planificar tu próximo viaje.
    </p>
    <a href="/mapa" style="
      display:inline-flex; align-items:center; gap:0.75rem;
      color:#00e5d4; background:#091d2b; text-decoration:none;
      font-size:0.85rem; font-weight:600; letter-spacing:0.12em;
      text-transform:uppercase; padding:1rem 2.5rem; border-radius:999px;
    ">Abrir mapa →</a>
  </section>

</Layout>

<style>
  .cascadas-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
  }
  .cascade-card__img-wrap {
    height: 300px;
    border-radius: 4px;
    margin-bottom: 1.25rem;
    position: relative;
    overflow: hidden;
  }
  .cascade-card__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  @media (max-width: 768px) {
    .cascadas-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
